# ── OS ───────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Editor ───────────────────────────────────────────────────
*.swp
*.swo
.idea/

# ── Supabase CLI temp ─────────────────────────────────────────
supabase/.temp/

# ── Claude memory & OpenSpec intentionally committed ─────────
# .claude/   ← 保留在版本控制
# openspec/  ← 保留在版本控制

# ── Claude local settings (per-machine, not shared) ──────────
.claude/settings.local.json
.gstack/

# ── Vendored agent skills (本機工具，非網站內容) ──────────────
.agents/
.claude/skills/
skills-lock.json
